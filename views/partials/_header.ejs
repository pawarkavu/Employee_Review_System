<header>
  <nav>
    <!-- here i am changing the removing the dead link on which i am currently. -->
    <% if(locals.urlPath.substring(0,6)==="/admin"){%> <a href="/employee">Employee dashboard</a> <%}%>
    <% if(locals.urlPath==="/employee" && locals.user.userType==="admin"){%> <a href="/admin">Dashboard | Admin</a> <%}%>
    <% if(locals.urlPath==="/employee" && locals.user.userType==="employee" && locals.user.listToReview.length>0){%> <a href="/admin">Review assigned Employee</a> <%}%>
  </nav>
  <!-- add user actual detatil -->
 <div class="user">
    <% if(locals.user)   {%>
      <a href=""><%= user.name %></a><a href="/user/session/destroy">Sign Out</a>
    <%}%>
 </div>  
 <% if(locals.urlPath!=="/"){%>
   <span class="btn menu">menu</span>
  <%}%>


</header>
